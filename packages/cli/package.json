{
  "name": "@opencortex/cli",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCortex command line client",
  "type": "module",
  "bin": {
    "cortex": "./src/index.js"
  },
  "scripts": {
    "build": "node --check src/index.js && node --check src/client.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
